# App MQTT

App mqtt deals with the default provided mqtt client and provides an intuitive way of publishing and subscribing messages.

It would return the user with an instance which has function pointers which can be used to send and recieve messages through mqtt.

