# HTTP Call
# Embed Token
To prevent calling API with email and password to obtain an access token, set ```CONFIG_ENABLE_IOTCORE_SERVER_API_EMBED_JSON = True```
using menuconfig. Then add token to a text file ```iotcore/embeddedresources/beef.txt``` 
