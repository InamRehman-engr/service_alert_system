if(CONFIG_ENABLE_OTA)
    list(APPEND srcs "${CMAKE_CURRENT_LIST_DIR}/src/esp_delta_ota.c")
    list(APPEND srcs "${CMAKE_CURRENT_LIST_DIR}/detools/c/detools.c")
    list(APPEND srcs "${CMAKE_CURRENT_LIST_DIR}/detools/c/heatshrink/heatshrink_decoder.c")
    list(APPEND includes "${CMAKE_CURRENT_LIST_DIR}/include")
    list(APPEND priv_includes "${CMAKE_CURRENT_LIST_DIR}/detools/c")
    list(APPEND priv_includes "${CMAKE_CURRENT_LIST_DIR}/detools/c/heatshrink")
    add_compile_options("-DDETOOLS_CONFIG_FILE_IO=0")
    add_compile_options("-DDETOOLS_CONFIG_COMPRESSION_NONE=0")
    add_compile_options("-DDETOOLS_CONFIG_COMPRESSION_LZMA=0")
    add_compile_options("-DDETOOLS_CONFIG_COMPRESSION_CRLE=0")
endif()